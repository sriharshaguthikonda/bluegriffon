name: Build BlueGriffon (Windows)

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["*"]

jobs:
  build-windows:
    runs-on: windows-2022
    steps:
      - name: Checkout BlueGriffon
        uses: actions/checkout@v4

      - name: Install MozillaBuild
        run: choco install -y mozillabuild

      - name: Build BlueGriffon executable
        shell: cmd
        run: |
          set "LOG=%GITHUB_WORKSPACE%\build.log"
          echo ==== build start %DATE% %TIME% ==== > "%LOG%"
          echo Workspace: %GITHUB_WORKSPACE% >> "%LOG%"
          dir "%GITHUB_WORKSPACE%" >> "%LOG%" 2>&1
          dir "C:\mozilla-build" >> "%LOG%" 2>&1
          if not exist "C:\mozilla-build\start-shell.bat" (
            echo ERROR: MozillaBuild start-shell.bat not found. >> "%LOG%"
            exit /b 2
          )
          call "C:\mozilla-build\start-shell.bat" -no-interactive -here -c "bash .github/scripts/build-bluegriffon-windows.sh" >> "%LOG%" 2>&1
          set "ERR=%ERRORLEVEL%"
          echo ==== build end (exit %ERR%) ==== >> "%LOG%"
          exit /b %ERR%

      - name: Upload executable
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bluegriffon-exe
          path: |
            gecko-dev/**/dist/bin/*griffon*.exe

      - name: Show build log tail
        if: always()
        shell: powershell
        run: |
          if (Test-Path build.log) { Get-Content build.log -Tail 200 }

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: ${{ github.workspace }}\build.log
